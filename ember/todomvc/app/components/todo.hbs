<li class="{{ if @todo.isCompleted 'completed' }}">
    <div class="view">
    <input
        class="toggle"
        type="checkbox"
        aria-label="Toggle the completion of this todo"
        checked={{ @todo.isCompleted }}
        {{ on 'change' (fn this.todos.toggleCompletion @todo) }}
    >
    <!--
    fn allows for partial application, which is similar to bind, but it never changes the invocation context;
    this is equivalent to using bind with a null first argument.
    -->
    <label>{{@todo.text}}</label>
    <button
        type="button"
        class="destroy"
        title="Remove this todo"
    ></button>
    </div>

    <input autofocus class="edit" value="Todo Text">
</li>
